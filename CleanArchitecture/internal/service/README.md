# Service 레이어

이 디렉토리는 **"Service" 용어를 선호하는 경우**를 위한 예제입니다.

## Service vs Use Case

### 동일한 역할
- ✅ 비즈니스 로직 처리
- ✅ 애플리케이션 흐름 조정
- ✅ Repository를 통한 데이터 접근

### 차이점: 용어뿐

```go
// Use Case 용어
type UserUseCase struct {
    userRepo UserRepository
}

// Service 용어
type UserService struct {
    userRepo UserRepository
}
```

## 어떤 용어를 사용할까?

### Use Case를 사용하세요 (권장)
- ✅ Clean Architecture의 공식 용어
- ✅ 명확한 의도 표현 ("무엇을 하는가")
- ✅ 비즈니스 시나리오 중심

### Service를 사용해도 됩니다
- ✅ 익숙한 용어
- ✅ 전통적인 아키텍처와 일관성
- ✅ 팀의 선호도에 따라

## 사용 방법

### 옵션 1: Service 디렉토리 사용
```bash
internal/
├── domain/
├── service/          # Use Case 대신
│   └── user_service.go
├── repository/
└── delivery/
```

### 옵션 2: Use Case 그대로 사용 (권장)
```bash
internal/
├── domain/
├── usecase/          # Clean Architecture 표준
│   └── user_usecase.go
├── repository/
└── delivery/
```

### 옵션 3: 둘 다 지원
```bash
internal/
├── domain/
├── usecase/          # 기본
│   └── user_usecase.go
├── service/          # 별칭
│   └── user_service.go  # UseCase를 래핑
├── repository/
└── delivery/
```

## 현재 프로젝트

**기본**: `internal/usecase/` (Use Case 용어)
**대안**: `internal/service/` (Service 용어 - 이 디렉토리)

둘 다 사용 가능하며, **내용은 완전히 동일**합니다!

## 결론

**이름은 중요하지 않습니다!**

중요한 것:
- ✅ 비즈니스 로직 분리
- ✅ 의존성 방향 (안쪽으로)
- ✅ 인터페이스 사용 (DIP)
- ✅ 테스트 가능성

팀에서 편한 용어를 선택하세요! 🎯

